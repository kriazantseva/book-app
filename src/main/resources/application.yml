spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_app?useSSL=false&serverTimezone=UTC
    username: root
    password: 55FromTheInside
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQLDialect

liquibase:
  change-log: classpath:db/changelog/db.changelog-master.yaml
